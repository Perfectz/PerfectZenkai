{
  "name": "perfectzenkai-api",
  "version": "1.0.0",
  "description": "Enterprise Azure Functions API for Perfect Zenkai with Key Vault integration",
  "main": "index.js",
  "scripts": {
    "start": "func start",
    "build": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@azure/identity": "^4.0.1",
    "@azure/keyvault-secrets": "^4.8.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@azure/functions": "^4.5.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "overrides": {
    "uuid": "^14.0.0"
  }
}
